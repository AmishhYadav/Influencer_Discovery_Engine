# Tech Stack

Since the project directory only contains the `.claude/` system tooling, there is no application code or defined core stack yet.

## Core Runtime Environment
*   **System**: macOS
*   **Tooling**: Node.js (for Claude GSD `.cjs` scripts), Git.

## Core Dependencies
*   No application dependencies (`package.json`, `requirements.txt`, etc.) found yet.

## Build and Tooling
*   **Version Control**: Git.
*   **Project Management**: Claude's "Get Shit Done" (GSD) workflow framework located in `.claude/`.

## Key Technologies
*   **Configuration**: JSON config files inside `.claude/`.
*   **Documentation**: Markdown files for GSD commands and workflows.
