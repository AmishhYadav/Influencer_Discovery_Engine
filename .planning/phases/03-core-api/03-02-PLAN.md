---
phase: 03-core-api
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - Creators can be listed and filtered by score
    - Briefings can be triggered via POST and retrieved via GET
  artifacts: []
  key_links: []
  requirements: [API-01, API-02, API-03]
---

# Phase 3 Plan 02: REST Endpoints & Async Briefings

## Objective
- **What:** Build the core routing tier (`creators` and `briefings` endpoints) including Pydantic schemas and FastAPI BackgroundTasks.
- **Why:** Exposes the discovery engine to the future frontend UI.
- **Output:** `src/api/routes.py` (or individual route files), `src/api/schemas.py`, and `src/api/tasks.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-api/03-CONTEXT.md
@.planning/phases/03-core-api/03-RESEARCH.md
@.planning/phases/03-core-api/03-VALIDATION.md

## Tasks

<task type="code">
  <name>3-02-01: API Schemas</name>
  <files>src/api/schemas.py</files>
  <action>Define Pydantic models for responses: `ChannelResponse`, `ChannelDetailResponse`, `BriefingResponse`, `BriefingRequest`. Ensure `from_attributes=True` (formerly `orm_mode=True`) is set to parse SQLAlchemy objects.</action>
  <verify>Syntax check only.</verify>
  <done>Schemas defined.</done>
</task>

<task type="code">
  <name>3-02-02: Creators Router</name>
  <files>src/api/routers/creators.py, tests/test_api_creators.py</files>
  <action>Implement `GET /api/creators` (params: `limit=20`, `offset=0`, `min_score=None`). Query returns `Channel` records sorted by `alignment_score` DESC. Implement `GET /api/creators/{channel_id}` returning full details including relationships if needed. Wire router into `main.py`. Add mocked DB tests using `TestClient`.</action>
  <verify>Run `pytest tests/test_api_creators.py`.</verify>
  <done>Discovery endpoints working and tested.</done>
</task>

<task type="code">
  <name>3-02-03: Briefings Router & Background Task</name>
  <files>src/api/routers/briefings.py, src/api/tasks.py, tests/test_api_briefings.py</files>
  <action>Create `src/api/tasks.py` with `generate_briefing_task(channel_id, db_url)`. It creates a DB session, fetches top chunks + quotes, calls OpenAI for a markdown briefing, and updates the `Briefing` record status to "completed" (or "failed"). In `routers/briefings.py`, add `POST /api/briefings/generate` which creates a `pending` Briefing in DB, adds `generate_briefing_task` to `fastapi.BackgroundTasks`, and returns `202 Accepted` with the `briefing_id`. Add `GET /api/briefings/{briefing_id}`. Wire into `main.py`.</action>
  <verify>Run `pytest tests/test_api_briefings.py` containing mocked background task tests.</verify>
  <done>Async briefing generation works silently in the background.</done>
</task>

## Verification
- Run `pytest tests/test_api_creators.py`
- Run `pytest tests/test_api_briefings.py`
- Manual verified via `/docs` Swagger UI.

## Success Criteria
- [ ] Endpoints implement API-01, API-02, and API-03
- [ ] Pydantic correctly serializes SQLAlchemy responses
- [ ] Background tasks update the database without blocking the HTTP response
