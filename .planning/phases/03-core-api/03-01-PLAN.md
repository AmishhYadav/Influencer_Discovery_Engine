---
phase: 03-core-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - FastAPI app runs and serves documentation at /docs
    - Database dependency can yield a SQLAlchemy session
    - Briefing model is added to the database
  artifacts: []
  key_links: []
  requirements: [API-01, API-02]
---

# Phase 3 Plan 01: FastAPI Foundation & DB Models

## Objective
- **What:** Setup the FastAPI application structure, CORS middleware, API router, DB dependency, and the new `Briefing` table.
- **Why:** We need a web server to host the REST endpoints, and a database table to store the async briefing results.
- **Output:** `src/api/main.py`, `src/api/deps.py`, and updates to `src/db/models.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-api/03-CONTEXT.md
@.planning/phases/03-core-api/03-RESEARCH.md
@.planning/phases/03-core-api/03-VALIDATION.md

## Tasks

<task type="code">
  <name>3-01-01: Update DB Models for Briefings</name>
  <files>src/db/models.py, src/db/dao.py, tests/test_db_briefing.py</files>
  <action>Add `Briefing` table to `src/db/models.py` (id, channel_id, content Text, status String "pending"|"completed"|"failed", created_at, updated_at). Add relationship on `Channel` (briefings). In `src/db/dao.py`, add `create_briefing(session, channel_id) -> Briefing` (starts pending) and `update_briefing(session, briefing_id, content, status)`. Add unit tests.</action>
  <verify>Run `pytest tests/test_db_briefing.py`.</verify>
  <done>Database can store and update async briefing results.</done>
</task>

<task type="code">
  <name>3-01-02: FastAPI App & Dependencies</name>
  <files>requirements.txt, src/api/main.py, src/api/deps.py</files>
  <action>Add `fastapi`, `uvicorn` to `requirements.txt`. Create `src/api/deps.py` with `def get_db()` that yields a Session from `DATABASE_URL` (requires moving engine creation logic out of CLI scripts to be importable, or just initializing a global engine in `deps.py`). Create `src/api/main.py` initializing a FastAPI `app` with CORS middleware open to all origins (for MVP). Add a basic `GET /health` route.</action>
  <verify>Run `pytest`: build a tiny test confirming `GET /health` works via `TestClient`.</verify>
  <done>FastAPI runs and can inject DB sessions.</done>
</task>

## Verification
- Run `pytest tests/test_db_briefing.py`
- Create and run `pytest tests/test_api_health.py`

## Success Criteria
- [ ] `Briefing` table exists
- [ ] Requirements include `fastapi` and `uvicorn`
- [ ] `get_db` dependency works
- [ ] FastAPI `app` object exists and serves `/health`
