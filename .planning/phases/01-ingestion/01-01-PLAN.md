---
phase: 01-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - YouTube API wrapper fetches channel metadata
    - Transcript wrapper fetches video transcripts
    - Cleaning utility strips filler and sponsor reads
  artifacts: []
  key_links: []
  requirements: [DATA-01, DATA-02, DATA-03]
---

# Phase 1 Plan 01: Core Ingestion Logic

## Objective
- **What:** Build the Python utilities for fetching YouTube metadata and transcripts, along with associated cleaning logic.
- **Why:** Delivers the raw ingestion capabilities required before we can store or integrate them into a CLI.
- **Output:** Python modules in `src/ingestion/` for YouTube API and transcript fetching, plus pytest coverage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingestion/01-CONTEXT.md
@.planning/phases/01-ingestion/01-RESEARCH.md
@.planning/phases/01-ingestion/01-VALIDATION.md

## Tasks

<task type="code">
  <name>1-01-01: YouTube API Wrapper</name>
  <files>src/ingestion/youtube_api.py, tests/test_youtube_api.py</files>
  <action>Create a class `YouTubeDataAPI` that takes an API key. Implement `search_channels(query)` to find channels and `get_latest_videos(channel_id, count=30)` to fetch recent video IDs via the playlistItems endpoint for a channel's uploads playlist. Implement retry logic with `tenacity`. Add mocked unit tests.</action>
  <verify>Run `pytest tests/test_youtube_api.py` and ensure tests pass.</verify>
  <done>Class exists in `src/ingestion/youtube_api.py` and mocked tests pass.</done>
</task>

<task type="code">
  <name>1-01-02: Transcript Fetcher</name>
  <files>src/ingestion/transcripts.py, tests/test_transcript_api.py</files>
  <action>Create `fetch_transcript(video_id)` using `youtube-transcript-api`. If transcripts are disabled, catch the exception, log it, and return None. Add mocked unit tests taking care of the `TranscriptsDisabled` scenarios.</action>
  <verify>Run `pytest tests/test_transcript_api.py` and ensure tests pass.</verify>
  <done>Function exists and robustly handles disabled transcripts.</done>
</task>

<task type="code">
  <name>1-01-03: Transcript Cleaning</name>
  <files>src/ingestion/cleaner.py, tests/test_cleaning.py</files>
  <action>Create `clean_transcript(transcript_list)` that filters out dictionaries where text contains known audio tags like `[Music]`, `[Applause]` and removes chunks indicating sponsor reads (e.g., checking for "NordVPN", "sponsor", "support the channel"). Add unit tests with mock transcript data.</action>
  <verify>Run `pytest tests/test_cleaning.py` and ensure it properly filters unwanted segments.</verify>
  <done>Function correctly strips filler and sponsor strings.</done>
</task>

## Verification
- Run `pytest tests/test_youtube_api.py`
- Run `pytest tests/test_transcript_api.py`
- Run `pytest tests/test_cleaning.py`

## Success Criteria
- [ ] YouTube API wrapper can fetch channel IDs and video lists
- [ ] Transcript API wrapper safely fetches transcripts or returns None
- [ ] Cleaning logic removes generic filler
- [ ] All automated tests pass
