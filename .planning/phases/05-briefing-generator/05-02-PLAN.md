---
phase: 05-briefing-generator
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [frontend/src/components/CreatorDetailSheet.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - Frontend exposes campaign context input.
    - Frontend provides a copy-to-clipboard button.
  artifacts: []
  key_links: []
  requirements: [UI-03, AI-01]
---

# Phase 5 Plan 02: Frontend UI Polish

## Objective
- **What:** Enhance the `CreatorDetailSheet` with a campaign context input and a copy-to-clipboard feature.
- **Why:** To allow coordinators to personalize briefings and easily export them for outreach.
- **Output:** Updated `frontend/src/components/CreatorDetailSheet.tsx`.

## Context
@.planning/PROJECT.md
@.planning/phases/05-briefing-generator/05-CONTEXT.md
@.planning/phases/05-briefing-generator/05-RESEARCH.md

## Tasks

<task type="code">
  <name>5-02-01: Add Campaign Context Input</name>
  <files>frontend/src/components/CreatorDetailSheet.tsx</files>
  <action>Add a `textarea` component above the "Generate Briefing" button. Bind it to a `campaignContext` state. Pass this state to the `generateBriefing.mutateAsync` call.</action>
  <verify>Open a creator sheet and ensure the textarea is interactive.</verify>
  <done>Campaign context can be provided by the user.</done>
</task>

<task type="code">
  <name>5-02-02: Implement Copy to Clipboard</name>
  <files>frontend/src/components/CreatorDetailSheet.tsx</files>
  <action>Add a "Copy to Clipboard" button that appears when `briefing?.status === 'completed'`. Use `navigator.clipboard.writeText(briefing.content)`. Add a temporary "Copied!" toast or tooltip state.</action>
  <verify>Generate a briefing, click copy, and paste it into a separate document.</verify>
  <done>Briefing content can be exported easily.</done>
</task>

## Verification
- Start the full stack (FastAPI + Vite).
- Navigate to a creator, enter context, generate, and copy.

## Success Criteria
- [ ] User can input custom context.
- [ ] Generated briefing is based on custom context.
- [ ] Copy button works as expected.
