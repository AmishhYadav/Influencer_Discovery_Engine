---
phase: 02-analysis-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - OpenAI generates embeddings for chunks
    - OpenAI evaluates alignment score and quotes via structured outputs
  artifacts: []
  key_links: []
  requirements: [NLP-01, NLP-02, NLP-03]
---

# Phase 2 Plan 02: OpenAI NLP Analysis

## Objective
- **What:** Build the core NLP engine using OpenAI's `text-embedding-3-small` and `gpt-4o-mini`.
- **Why:** This calculates the critical 0-100 alignment score and extracts quotes, fulfilling the core value proposition.
- **Output:** `src/analysis/nlp.py` containing embedding and scoring functions, plus a Pydantic schema for the LLM output.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-analysis-engine/02-CONTEXT.md
@.planning/phases/02-analysis-engine/02-RESEARCH.md
@.planning/phases/02-analysis-engine/02-VALIDATION.md

## Tasks

<task type="code">
  <name>2-02-01: OpenAI Embedding Service</name>
  <files>src/analysis/nlp.py, tests/test_openai_api.py</files>
  <action>Add `openai` to dependencies. Create `get_embedding(text: str) -> list[float]` using `text-embedding-3-small`. Create a batch version `get_embeddings(texts: list[str])`. Add mocked tests.</action>
  <verify>Run `pytest tests/test_openai_api.py` focusing on embedding mocks.</verify>
  <done>Can reliably fetch 1536-dim vectors from OpenAI API.</done>
</task>

<task type="code">
  <name>2-02-02: LLM Scoring Judge</name>
  <files>src/analysis/nlp.py, tests/test_openai_api.py</files>
  <action>Define a Pydantic model `AlignmentResult` (alignment_score: int 0-100, reasoning: str, quotes: list of {text, timestamp}). Implement `score_chunks(chunks: list[dict], target_topic: str) -> AlignmentResult`. Build a prompt that instructs `gpt-4o-mini` to evaluate the chunks for "soft activism" and topic alignment (rewarding alignment, heavily penalizing polarizing/protest language). Use `.parse()` for Structured Outputs. Mock the OpenAI client in tests.</action>
  <verify>Run `pytest tests/test_openai_api.py` focusing on chat completion mocks.</verify>
  <done>Function returns strongly-typed AlignmentResult based on the chunks.</done>
</task>

## Verification
- Run `pytest tests/test_openai_api.py`

## Success Criteria
- [ ] Text chunks can be converted to embeddings
- [ ] LLM evaluates chunks and strictly outputs a 0-100 score + quotes using Pydantic
- [ ] System prompt penalizes explicit activism while rewarding topic alignment
